#include <stdio.h>

int main(void)
{
	int n, m, i, t, sum, average, prime;

	while (scanf("%d %d", &n, &m) != EOF)
	{
		sum = 0;
		average = 0;
		for (i = 0; i < n; i++)
		{
			t = 2 * n + 2;
			sum += t;
		}
	}

	return 0;
}